Build 
=============
Build enterprise AI agents in Sparkflows using visual agentic workflows that combine AI models, enterprise data, tools, business logic, and actions.

Sparkflows provides a visual environment for designing agent behavior and execution logic without requiring the entire application to be assembled in code. Using reusable processors and workflow components, teams can connect agents to enterprise data, configure LLMs, add tools and retrieval, incorporate AI/ML models, and define the steps an agent follows to complete a task.

Agents can be designed for a focused task or used as part of a larger orchestrated process. A workflow can combine deterministic rules and transformations with AI-driven reasoning, allowing data preparation, model predictions, retrieval, tool calls, approvals, and agent decisions to operate within the same flow.

Start Here
=================
To start building, use the Quickstart to create an agent, define its instructions, connect the required data and tools, run the workflow, and review the result. You can start from scratch in the visual builder, adapt a reusable template, use Sparkflows AI Assistant to generate an initial workflow from natural language, or extend the flow with code and custom processors when required.

After the initial agent works end to end, use Test & Debug to inspect execution before moving to evaluate for systematic validation and deploy for production use.

Documentation Topics
=========================

.. list-table::
   :widths: 30 70
   :header-rows: 1

   * - Topic
     - Description
   * - Quickstart
     - Build, run, test, and prepare your first AI agent for deployment.
   * - Agent Builder
     - Create and configure agents using Sparkflows visual workflow environment.
   * - Agent Architecture
     - Understand how models, prompts, data, tools, workflows, and actions work together.
   * - Create an Agent
     - Define an agent's role, goal, instructions, inputs, outputs, and execution behavior.
   * - Agentic Workflows
     - Design multi-step agent processes using visual workflows.
   * - Workflow Processors
     - Use reusable processors for AI, data, logic, tools, and business actions.
   * - Configure LLMs
     - Connect and configure supported LLM providers and models.
   * - Add Tools to Agents
     - Give agents access to APIs, workflows, databases, applications, and custom tools.
   * - Connect Enterprise Data
     - Use enterprise data sources directly within agent workflows.
   * - RAG & Knowledge
     - Incorporate predictive models and ML outputs into agent decisions.
   * - Multi-Agent Workflows
     - Coordinate specialized agents within larger business processes.
   * - Human-in-the-Loop
     - Add review, approval, and escalation steps to agent workflows.
   * - Custom Processors & Code
     - Extend workflows with custom processors, Python, SQL, and JavaScript.
   * - Authentication & Secrets
     - Secure access to protected data, APIs, applications, and credentials.
   * - Error Handling & Recovery
     - Define behavior for failed model calls, tools, data operations, and workflow steps.
   * - Test & Debug
     - Run agents, inspect execution, and troubleshoot workflow issues.
   * - Troubleshooting
     - Diagnose common issues when building and configuring agents.
