Database Authentication
=======================

Sparkflows can authenticate the user against its own database.

Users' passwords are encrypted and stored.

This is the default authentication mechanism of Sparkflows. Users created in Fire are stored in the database.
